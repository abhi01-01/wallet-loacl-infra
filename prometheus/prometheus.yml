global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'wallet-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # host.docker.internal allows the container to read port 8081 on your physical machine
      - targets: ['host.docker.internal:8081']